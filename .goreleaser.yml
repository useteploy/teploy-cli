version: 2

builds:
  - main: ./cmd/teploy
    binary: teploy
    env:
      # Without this, the linked-against-glibc Linux binary fails to run
      # at all inside any musl-based container (Alpine) with a bare "no
      # such file or directory" — the actual error is a missing dynamic
      # linker, not a missing file. Found live: a freshly downloaded
      # release binary failed this way inside Alpine, breaking every
      # delegate-to-CLI action in a containerized teploy-dash deployment.
      # teploy-dash and teploy-observe's own goreleaser configs already
      # set this; teploy-cli's never did.
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]
    name_template: "teploy_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt

changelog:
  sort: asc

# NOTE (2026-05-29): goreleaser 2.16 flags `brews:` as deprecated but does
# not yet ship a 1:1 replacement (homebrew_casks rejects `install:`; no
# homebrew_formulae key exists). Leaving as-is until goreleaser publishes a
# clean migration target; release path still works (deprecation, not error).
brews:
  # Pin the formula name to `teploy` (the binary + what users install). Without
  # this, goreleaser derives it from the project/repo name — which became
  # `teploy-cli` after the repo rename and split the formula (a stale
  # `teploy-cli.rb` at 0.1.5 alongside the real `teploy.rb`). Keep it `teploy`.
  - name: teploy
    repository:
      owner: useteploy
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/useteploy/teploy-cli
    description: Zero-downtime Docker deploys to any server via SSH
    license: MIT
    install: |
      bin.install "teploy"

scoops:
  - name: teploy
    repository:
      owner: useteploy
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/useteploy/teploy-cli
    description: Zero-downtime Docker deploys to any server via SSH
    license: MIT
